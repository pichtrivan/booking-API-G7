# Application Name
spring.application.name=booking-api

# Server Port
server.port=8081

# Datasource (PostgreSQL)
spring.datasource.url=jdbc:postgresql://ep-flat-heart-a1ww7nap-pooler.ap-southeast-1.aws.neon.tech:5432/booking-api?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_qaXfly27mDFB
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Logging Levels
logging.level.org.springframework=INFO
logging.level.com.project=DEBUG

# Disable default static resource handling
spring.web.resources.add-mappings=false
